# Cursor 하네스 엔지니어링

이 문서는 **에이전트 행동을 설계·점검하는 하네스 엔지니어링** 관점에서, 본 저장소의 Cursor 설정을 한곳에 정리한다. 규칙의 철학은 안드레 카파시가 LLM 코딩에서 반복되는 실패를 [정리한 관찰](https://x.com/karpathy/status/2015883857489522876)과, 이를 행동 지침으로 옮긴 [Karpathy Guidelines](https://github.com/forrestchang/andrej-karpathy-skills/blob/main/skills/karpathy-guidelines/SKILL.md)의 의도와 맞춘다.

> **트레이드오프:** 속도보다 신중함을 우선한다. 사소한 작업은 합리적으로 완화한다.

---

## 1. 하네스가 막는 것

에이전트의 기본 경향은 대략 다음과 같다. 하네스는 이를 **구조적으로 상쇄**한다.

| 경향 | 하네스 대응 |
|------|-------------|
| 가정을 조용히 깔고 간다 | 구현 전 사고: 가정·모호함·트레이드오프를 드러낸다 |
| 과하게 일반화·추상화한다 | 단순성 우선: 요청 밖 코드를 쓰지 않는다 |
| diff 범위를 넓힌다 | 정밀한 수정: 손댄 줄이 요청과 직결되게 한다 |
| “됐다”의 기준이 약하다 | 목표 중심 실행: 검증 가능한 성공 기준을 둔다 |

---

## 2. 저장소 안에서의 역할 분담

| 산출물 | 역할 |
|--------|------|
| `.cursorrules` | **항상 주입되는 실행 하네스.** 짧은 명령형 규칙. |
| `CLAUDE.md` | **긴 설명·예시·출처.** 사람·에이전트 모두 참고. |
| 이 파일 (`CURSOR..md`) | **왜 이렇게 묶었는지, 점검 관점.** 온보딩·리뷰용. |

규칙을 바꿀 때는 **세 파일이 서로 모순되지 않게** 맞춘다. 실행 문구는 `.cursorrules`에, 배경과 링크는 `CLAUDE.md`에 두는 편이 좋다.

---

## 3. 네 가지 원칙 (카파시 의도 요약)

### 3.1 구현 전 사고 (Think Before Coding)

**가정하지 않는다. 모호함을 숨기지 않는다. 트레이드오프를 드러낸다.**

- 가정은 말로 밝힌다. 불확실하면 질문한다.
- 해석이 여러 개면 임의로 고르지 않고 대안을 나열한다.
- 더 단순한 해법이 있으면 제안한다. 타당하면 요청에 푸시백해도 된다.
- 불분명하면 멈추고, 무엇이 불분명한지 구체적으로 짚는다.

### 3.2 단순성 우선 (Simplicity First)

**문제를 푸는 데 필요한 최소한의 코드만 쓴다. 추측성 코드는 넣지 않는다.**

- 요청 범위를 넘는 기능·설정·유연성을 추가하지 않는다.
- 일회성 코드용 추상화 계층을 만들지 않는다.
- 현실적으로 불가능한 시나리오만을 위한 방어 코드를 넣지 않는다.
- 더 짧게 쓸 수 있으면 다시 쓴다.
- **자문:** “시니어 엔지니어가 이걸 과하게 복잡하다고 할까?”

### 3.3 정밀한 수정 (Surgical Changes)

**꼭 필요한 곳만 건드린다. 정리는 자기가 만든 잔여물만 한다.**

- 인접 코드·주석·포맷을 “손대며 정리”하지 않는다.
- 망가지지 않은 부분은 리팩터링하지 않는다.
- 기존 스타일·관례를 따른다.
- 무관한 데드 코드는 **언급만** 하고 삭제하지 않는다.
- **자기 변경**으로 불필요해진 import·변수·함수만 제거한다. 원래 있던 데드 코드는 요청 없이 지우지 않는다.
- **검증:** 변경된 모든 줄이 사용자 요청과 직접 연결되어야 한다.

### 3.4 목표 중심 실행 (Goal-Driven Execution)

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

모호한 일을 구체 목표로 바꾼다.

- “유효성 검사 추가” → 잘못된 입력에 대한 테스트를 쓰고 통과시킨다.
- “버그 수정” → 재현 테스트를 쓰고 통과시킨다.
- “X 리팩터링” → 전후로 테스트(또는 합의된 검증 수단)가 통과한다.

여러 단계일 때는 짧은 계획과 단계별 검증을 밝힌다.

```text
1. [단계] → 검증: [확인 사항]
2. [단계] → 검증: [확인 사항]
3. [단계] → 검증: [확인 사항]
```

“일단 돌아가게”만 두면 되묻기가 반복된다. 기준을 문장으로 고정한다.

---

## 4. 하네스 품질 점검

- diff에 요청과 무관한 변경이 남지 않았는가.
- 과설계로 되돌리는 일이 줄었는가.
- 구현 전에 질문·트레이드오프가 먼저 나왔는가.
- 성공 기준이 **재현 가능한 행동**(테스트, 빌드, 수동 시나리오 등)으로 적혀 있는가.

---

## 5. 출처

- [Andrej Karpathy (X)](https://x.com/karpathy/status/2015883857489522876)
- [karpathy-guidelines / SKILL.md](https://github.com/forrestchang/andrej-karpathy-skills/blob/main/skills/karpathy-guidelines/SKILL.md)
- 저장소 내 상세 본문: `CLAUDE.md`
